# 판단 기록 — 한 줄에 하나

- 작업 가지는 세션이 지정한 `claude/fcdochak-phase-1-0n4xlc` 하나로 하고, 단계는 커밋으로 나눴다(`fcdochak-<단계>` 가지 여러 개 대신). PR 은 `fcdochak` 으로.
- `fcdochak` 기준 가지는 `main` 에서 새로 떴다. `main` 과 저장소 뿌리 파일은 건드리지 않았다(뿌리에 도구 설정이 없어 예외가 필요 없었다).
- 표는 `public` 이 아니라 `fcd` 스키마에 둔다 — Supabase PostgREST 에 기본 노출되지 않게.
- Supabase 의 `anon`/`authenticated` 대신 자체 역할 `fcd_user`·`fcd_public` 로 `set local role` 해서 RLS 를 받는다. 앱은 서버에서만 DB 에 붙는다.
- 로그인 확인은 Supabase Auth(있으면)·로컬 scrypt(없으면), 세션은 자체 서명 쿠키(`fcd_session`, HS256). 브라우저에 Supabase 키를 두지 않는다.
- `fcd.profiles.id` 는 `auth.users` 를 FK 로 참조하지 않는다 — 로컬 PGlite 와 같은 스키마로 돌리고, 데모 걷어내기를 한 SQL 로 끝내려고.
- 환경변수가 없으면 PGlite 로 끝까지 돈다(시험·미리보기·로컬). 두 드라이버의 값 모양(date·timestamptz·int8)을 같게 맞췄다.
- 마이그레이션은 SQL 파일을 빌드 때 TS 로 묶어(`migrations.generated.ts`) 서버리스에서도 파일 없이 올린다. 시험이 둘이 같은지 본다.
- 거래 기록 표(요금표·응찰·청구·설정·관세율·등급)는 UPDATE 권한 자체가 없다. 정정은 `supersedes_id` 로 새 판을 쌓는다. 상태 칸만 바뀌는 표(요청·예외·광고 등)는 따로 둔다.
- 삭제 요청 처리 = 업체 상태를 `deleted` 로 바꾸기(공개 면에서 사라짐). 실제로 지우는 SQL 은 만들지 않았다.
- 참조표(거점·항구·FC·구간·화물 성격)는 코드가 원본이라 `db:migrate` 때 upsert 한다. 설정·관세율은 키가 없을 때만 첫 판을 넣고, 운영자가 쌓은 판은 덮지 않는다.
- 돈 계산은 `src/lib/money` 의 순수 함수만. BigInt 로 자릿수를 고정하고 반올림은 사사오입, 세금은 원 단위 버림.
- 성사 수수료 기준 = 물류비 합계 − 관세사 보수, 관세·부가세 제외(관세사법 제3조 제2·3항). 시험으로 고정.
- 관세·부가세는 「참고 추정」으로만 보이고 목록통관은 다루지 않는다.
- 업체가 맡지 않는 구간은 플랫폼 참고치(설정 `reference_lines`)로 채워 합계를 보이되, 점선 무늬로 구분하고 확정 합계에서는 뺀다.
- 비교 목록은 운송(freight) 구간을 포함한 요금표만 올린다 — 운송이 없는 요금표는 같은 조건 비교가 안 된다.
- 비로그인 계산기는 「공개 가격」으로 표시한 요금표만 쓰고, 금액 대신 구간 비중(천분율)만 돌려준다.
- 추천 점수 = 정시 30 · 청구 편차 25 · FC 회송 25 · 가격 확정도 20. 실측이 없으면 중립 0.5. 광고·특수관계는 점수에 넣지 않는다.
- 광고는 한 구간·기간에 한 자리만(서버에서 겹침 검사), 목록 맨 위에 「광고」 표시.
- 「FC 입고 준비 인증」 기준(60건·3.5%)은 설정 표에서 읽는다. 쿠팡 상표는 쓰지 않는다.
- 공개 정보 업체(미등록)는 이름·지역·업종·출처만. 가격·소개·사진·개인 연락처를 싣지 않는다.
- 물류사는 경쟁사의 비공개 요금표를 볼 수 없다(`fcd.i_am_shipper()` 로 화주에게만 비교용 공개) — 시험 있음.
- 화주 이름은 예약 전까지 물류사에 보이지 않는다(orgs RLS).
- 9구간 막대의 색은 순서(한 색상 명도 단계, 밝음→짙음)만 싣고, 확정도는 무늬(예상 빗금·추가비용 호박 테·참고치 점선)로 싣는다 — 색각 이상에서도 읽히게. 팔레트는 validate_palette 로 밝은·어두운·남색 위 세 벌 검사.
- 차트는 한 계열·한 축만. 두 축 차트를 쓰지 않았다.
- 큰 숫자·브랜드·공개 제목에는 Black Han Sans 를 쓴다(요청 사항). 본문·표의 숫자는 Pretendard 고정폭 숫자.
- 글꼴은 저장소에 묶었다(Pretendard 하위집합·Black Han Sans, OFL). 외부 CDN 을 부르지 않는다.
- frontend-design 스킬은 이 환경에 없어, DESIGN.md 를 먼저 쓰고 캡처 검토를 두 번 돌리는 방식으로 대신했다.
- next-intl 은 물류사 콘솔에만(ko/zh). 쿠키 `fcd_locale` → 사용자 설정 → ko 순.
- 밖으로 나가는 것은 `sendOutbound` 한 곳을 지나고 기본 꺼짐. 앱 안 알림(DB)은 늘 쌓인다. Supabase Storage 올리기는 우리 저장소라 스위치 밖에 둔다.
- 서류 파일은 비공개 버킷 + 짧은 서명 주소로만 내려준다. Supabase 가 없으면 기록만 남기고 파일은 받지 않는다.
- 공개 계산 API 는 IP 당 분당 90회로 막는다(메모리 기준 — 서버리스 인스턴스마다 따로 셈).
- 데모 조직 규모는 「최근 40건·진행 60건」에 더해 90일 추이를 그릴 지난 기록을 함께 넣었다(점수·편차가 실측에서 나오게).
- 데모 운영자도 데모 운영 조직(is_demo) 아래에 둬 걷어내기 한 번에 사라지게 했다.
- `demo:purge` 는 SQL 파일만 만든다. Supabase `auth.users` 정리도 같은 파일에 id 목록으로 넣는다.
- `DEMO_MODE` 는 실행 때 읽지만, 공개 쪽 미리 그린 쪽 때문에 바꾸면 다시 배포하라고 문서에 적었다.
- 공개 쪽 Lighthouse·캡처는 로컬 `next start` 에서 쟀다(배포 주소가 아직 없음).
- 모바일 전체 캡처에서 아래 탭 막대가 고정이라 캡처 중간에 겹쳐 보이는 것은 실제 화면 문제가 아니라 그대로 뒀다.
- 글꼴은 unicode-range 조각(Pretendard 92·Black Han Sans 120) 대신 KS X 1001 한글 2,350자+라틴·기호 한 파일씩으로 줄였다 — 조각마다 배치 비용(파일당 약 18ms)이 붙어 모바일 Lighthouse 성능이 60~70대였다. 드문 음절·한자는 시스템 글꼴로 넘어간다(`scripts/fonts/build.sh`).
- 글꼴(640KB)은 첫 화면 경로에서 뺐다: 처음 온 기기는 그 쪽을 시스템 글꼴로 끝까지 그리고 한가할 때 파일만 캐시에 받아 두며, 다음 쪽 로드부터 머리에 글꼴 CSS 를 파싱 중에 써 넣어(캐시에서) 처음부터 붙인다(`layout.tsx` fontScript — optional 은 첫 그리기 뒤엔 영영 안 바뀌어 쓰지 않음). 로드 뒤 바꿔 끼우면 CLS 0.2, 미리 받기(preload)는 느린 4G 에서 LCP 6초라 둘 다 뺐다.
- 로그인 쪽은 검색에 열어 둔다(공개 쪽). robots 의 `/partner` 가 공개 `/partners` 까지 막던 것을 `/partner$`·`/partner/` 로 좁혔다. 청록(--ok) 밝은 값은 칩 배경 위 대비 4.5 를 넘기려 #0C7166 으로 한 단계 짙게.
- 공개 첫 화면의 첫 화면 밖 구역과 바닥글은 `content-visibility: auto` — 모바일에서 보이지 않는 구역 배치를 미뤄 성능 70대→90대.
- postgres.js 는 jsonb 자리에 넘긴 JSON 글자를 한 번 더 JSON 으로 감싼다 — 2026-09-25 첫 Supabase 시드(설정·응찰·청구 금액 등)가 글자로 감싸여 저장됐다. 드라이버가 쓰기는 다시 감싸지 않게, 읽기는 한 겹 벗기게 고쳐 저장된 자료는 건드리지 않았다(지우거나 덮는 SQL 을 돌리지 않는 규칙). SQL 쪽에서 jsonb 안을 읽는 곳은 없다.
- 배열(text[]·int[])도 드라이버가 직접 푼다 — 풀러 뒤에서 postgres.js 가 배열 형식을 못 알아내는 경우 대비.
- 실제 Postgres 드라이버 경로 시험: PGlite 소켓 서버(@electric-sql/pglite-socket, maxConnections 50)에 postgres.js 로 붙여 마이그레이션·시드·next build·smoke 10개를 돌렸다.
- Vercel 빌드는 미국(iad1)에서 돌고 운영 DB 는 서울이라 왕복이 길다 — 구간 시세(`laneStats`)가 구간마다 요금표를 따로 읽어 구간 화면 미리 만들기가 60초를 넘겼다. 모든 구간 요금표를 한 번에 읽고, 같은 결과를 60초 동안 나눠 쓰며, `staticPageGenerationTimeout` 을 180초로 둔다. 업체 화면(`/p/[slug]`)도 요금표를 한 번에 읽는다. 시험: 실제 Postgres 16 앞에 왕복 180ms 지연 프록시를 두고 빌드 — 고치기 전 60초 초과 25건·실패, 고친 뒤 76장 36초.
- v2 p0 — 기준 화물은 `src/lib/standard-cargo.ts` 한 곳(3 CBM·650 kg·40박스·1,200개·24,000 RMB, 이우→인천·이천 FC). 계산기 첫 값·URL 기본값·구간 시세·업체 공개 요금·물류사 시장 데이터가 모두 이것을 읽고, 계산기 총액 옆에 「이 조건 기준」과 조건 요약을 적는다(예전 첫 값 3.6 CBM·820 kg 은 공표 기준과 달랐다). 시험이 옛 값·따로 박은 상수를 찾는다.
- v2 p0 — 데모 메뉴가 눌러도 이동하지 않던 원인: Radix 메뉴 항목 안의 `<form>` 이 항목을 누르는 순간 메뉴와 함께 문서에서 빠져, 브라우저가 「Form submission canceled because the form is not connected」로 제출을 취소했다(POST 가 나가지 않음 — 개발 서버에서 확인). 폼을 메뉴 밖(늘 붙어 있는 자리)에 두고 항목의 onSelect 가 `requestSubmit()` 한다.
- v2 p0 — 특수관계 업체(`orgs.related_party_note`)는 공개 계산기·비교·판매손익의 순위에서 기본으로 뺀다. 「특수관계 포함」으로 켜면 넣고, 그때 1위가 특수관계면 경고 띠. 점수 계산에는 여전히 넣지 않는다. 정렬·특수관계 처리는 순수 함수 `src/lib/ranking.ts`.
- v2 p0 — 공개 계산기 정렬 기본은 가격순(예전 동작 그대로), 「추천 점수순」으로 바꿀 수 있고 현재 기준을 글로 적는다. 비교 화면 정렬 이름도 같은 말(`SORT_LABEL`).
- v2 p0 — 합계는 「확정 합계」(업체가 확정으로 준 칸)와 「참고치 포함 합계」(= 총액)를 따로 적고, 그 사이를 「업체 예상」「참고치」로 나눠 보인다(`money/totals.ts`, 확정+예상+참고치 = 참고치 포함 합계). 큰 숫자는 참고치 포함 합계 그대로 두고 이름을 붙였다.
- v2 p0 — 9구간 막대 대체 글은 한 문장 요약(합계·가장 큰 구간·칸 수)만, 구간별 숫자는 화면 읽기용 표(`aria-describedby`)로 나눴다. 옆에 보이는 표(NineTable)가 이미 있는 곳은 `table="none"`. 비로그인 천분율은 %로 읽힌다(예전엔 「412원」처럼 읽힐 수 있었다).
- v2 p0 — 홈 H1 은 보이는 글과 같은 문장을 `aria-label` 로 준다(색 강조 조각이 읽히는 이름에서 빠지지 않게).
- v2 p0 — 미래 날짜 후기: 데모 시드가 청구일 뒤 1~5일로 후기를 찍으며 오늘 뒤로 넘어간 것(시험으로 재현). 시드는 「지금 − 1시간·오늘 KST 끝」을 넘지 않게 자르고, 공개 후기 쿼리(`reviews-query.ts`)와 화면(`notFuture`)에서도 오늘(KST) 이후를 뺀다. 저장된 자료는 고치지 않았다(읽을 때 거른다).
- v2 p0 — 미리보기 띠는 서버 환경변수 `PREVIEW_BANNER` 가 있을 때만 루트 레이아웃 맨 위(모든 화면). 공개 쪽은 미리 그리므로 값은 빌드 때 정해진다 — 바꾸면 다시 배포.
- v2 tools — 공개 판매손익 계산기 `/tools/pnl`. 돈 계산은 `money/seller.ts`(`sellerPnl`)가 `unitPnl`·`breakEvenPrice`·`sensitivity` 를 그대로 부른다: 광고비(판매가 × 광고비율)는 판매 수수료와 같은 「판매가 비례 비용」으로 합쳐 넘기고(화면에는 둘을 나눠 적되 합이 원 단위까지 같게), 로켓그로스 입출고비+배송비는 개당 풀필먼트로, 특성 추가비용(전체)은 물류비에 얹는다.
- v2 tools — 쿠팡 기준값은 설정 표 새 키 `tools.coupang_fee_basis`(참조 시드에 덧붙임, 없을 때만 첫 판). 실제 쿠팡 요율을 확인한 값이 아니므로 `example: true` 로 두고 화면에 「예시 기준값 · 확인일 2026-09-25」를 적는다. 값은 기존 `sale_fee_bp`(10.8%)·`fulfillment_per_unit`(2,800원)과 어긋나지 않게 입출고 700 + 배송 2,100 으로 나눈 예시이고, 광고비율은 모르는 값을 짓지 않으려 0. 키가 없으면(운영 DB 에 시드 전) 옛 기본값 + 「확인일 없음」으로 돈다. 「확인한 기준값」은 `example:false` 와 확인일이 함께 있을 때만.
- v2 tools — 특성 추가비용은 금액 없이 항목 글만(`tools.trait_extra_costs`) — 인증·검사·할증 금액은 업체·결과마다 달라 지어내지 않는다. 합계에 자동으로 넣지 않고, 아는 금액은 「특성·기타 추가비용」 칸에 직접 넣게 했다. 같은 표를 홈 계산기·비교 화면의 사전 판정 아래 「추가비용 가능」으로도 보인다.
- v2 tools — 「구간 시세로 도착원가」(`/api/tools/arrival`, IP 당 분당 60회)는 구간 시세(`laneStats`)와 같은 규칙(공식·인증 대기 업체, 지금 유효한 요금표, `asSystem` 으로 읽고 집계만 내보냄) — 기준 화물이면 구간 시세 중간값과 같다(시험). 사용자 화물로 다시 계산하고 특성으로 취급 못 하는 업체를 뺀 뒤 중간값·최저·싼 쪽 4분의 1·구간별 중간값만 싣고, 뺀 업체는 이름과 사유만(가격 없음). 관세 과세가격에 넣는 「도착항까지 운임」은 업체별 도착항까지 합의 중간값.
- v2 tools — 공개/가입 후 선: 구간별 9구간 중간값·합계·최저·업체 이름·판매손익 계산기는 공개, 업체별 9구간 가격 비교와 견적 요청은 가입 후. 구간 목록·구간 화면에 같은 안내(`OpenGate`)를 두고 「가격은 가입 후」를 「업체별 가격과 견적 요청은 가입 후」로 고쳤다. 홈 계산기의 공개 가격 업체 총액(요금표에 「공개 가격」 표시한 곳)은 그대로다.
- v2 tools — 홈 계산기 응답(`buildQuoteResponse`)에 뺀 업체 이름·사유(`excludedList`)를 덧붙였다 — 전에는 「뺀 곳 N」 숫자만 있었다. 공개 머리 메뉴에 「판매손익 계산」, 바닥글·홈 도구 띠·홈 계산기·구간 화면에서 계산기로 조건을 실어 보낸다(`?lane=` 또는 `?hub·port·mode·화물·traits`).
- v2 check — 청구서 점검(`/check`)은 공개·비로그인으로 결과만 보이고 저장하지 않는다. 비교 기준은 셀러가 넣은 화물로 그 구간의 현재 요금표(공식·인증 대기, 운송 포함)를 그때 계산한 구간별 분포(`src/lib/invoice-market.ts`, 앱에서는 asSystem 으로 좁게) — 기준 화물 하나로 미리 계산하는 `laneStats` 는 건드리지 않았다. 내보내는 것은 집계(중간값·싼 쪽 25%·최저)뿐이고, 그 구간을 맡은 요금표가 `invoice_check_rule.minSamples` 보다 적으면 플랫폼 참고치로 바꾸고 최저를 싣지 않는다(한 업체 가격이 드러나지 않게).
- v2 check — 판정선은 설정 `invoice_check_rule`(참조 시드에 첫 판): 과함 = 중간값 +20% 이상이면서 비싼 쪽 25% 경계 초과(참고치 기준이면 경계 조건 없음) · 낮음 = 중간값 −30% 이하 · 빠짐 = 청구서에 없는데 요금표 50% 이상이 맡는 구간(참고치 기준이면 비율을 몰라 빠짐으로) · 따로 = 그 밖의 없는 구간 · 비로그인 분당 20회(IP, 메모리). 「상위 25%」는 구간 시세 화면처럼 「싼 쪽 25%」로 적었다(상위가 비싼 쪽으로 읽힐 수 있어서).
- v2 check — 방식 「상관없음」이면 항공·해상 요금표가 한 분포에 섞여 비싼 쪽 25% 경계가 크게 올라간다(e2e 에서 확인). 방식을 고르라는 안내만 두고 판정 규칙은 그대로 뒀다.
- v2 check — 항목 이름 → 9구간 분류는 순서 있는 낱말 규칙(`src/lib/invoice-parse.ts`, 한·중·영). 관세·부가세는 물류비가 아니라 「세금」으로 빼고, 못 정한 줄은 합계에만 넣는다. 셀러가 옆 칸에서 바꿀 수 있다. 엑셀·붙여넣기 내용은 브라우저에서 읽고 파일을 서버에 두지 않는다.
- v2 check — 보관(`fcd.invoice_checks`, 0006)은 본인만 읽는다(같은 조직 동료도 못 봄 — 개인이 올린 청구서). UPDATE·DELETE 권한 없음, 고쳐서 다시 보관하면 `supersedes_id` 새 판(한 판 뒤에 하나), 목록은 `v_invoice_checks_current`. 보관할 때 서버가 다시 계산해 넣는다(화면 결과를 믿지 않음). 데모 화주에게 점검 3건(수정본 새 판 포함)을 시드했고 `DEMO_TABLES` 에 넣었다.
- v2 check — 홈 첫 화면의 노란 주 버튼은 「내 견적서·청구서 점검받기」 하나로, 계산기의 「9구간 상세·견적 요청은 가입 후」는 보조 버튼으로 내렸다(한 화면에 주 행동 하나).
- v2 trust — 추천 점수를 항목별(정시 입고·청구 편차·FC 회송·가격 확실성)로 보인다: 업체 화면(`/p/[slug]`)·비교 줄·물류사 「후기·답변」에 같은 컴포넌트(`ScoreBreakdown`, 순수 함수 `scoreBreakdown`). 업체 화면의 가격 확실성은 요금표마다 다르므로 최근 180일 응찰의 확정 구간 비중(`v_partner_metrics.price_certainty`)으로 계산하고 그렇게 적는다.
- v2 trust — 표본 기준은 설정 `score_min_sample`(기본 최근 30일 끝난 선적 20건). 표본 = 그 기간에 끝난 선적(입고·회송·반려·분실). 모자라면 점수·항목 점수를 싣지 않고 「표본 부족(N건)」과 잰 값만. 추천순에서는 표본이 모자란 업체를 점수 있는 업체 뒤에 둔다(보이지 않는 점수로 앞세우지 않는다) — 가격순 등 다른 정렬은 그대로.
- v2 trust — 청구 편차 「나쁜 쪽 10%」는 청구가 가장 많이 늘어난 상위 10% 건(ceil(n×0.1), 최소 1건)의 평균이다. 90번째 백분위수는 20건 중 2건이 크게 튀어도 보간으로 묻혀(시험 재현: 1.65%) 쓰지 않았다. 편차 목록은 `fcd.partner_trust_facts` 가 숫자 배열로만 낸다(선적·화주를 가리키는 값 없음), 계산은 `money/trust.ts`.
- v2 trust — 선적 상태값은 9단계·회송 수량·예외뿐이고 「분실」 상태값이 없다. 그래서 끝(outcome)을 기록에서 읽는다: 9단계=입고 완료/회송 있음, 9단계 전 + 「FC 입고 반려」 예외=반려, 9단계 전 + FC 도착 예정일 + 설정 `review_lost_after_days`(14일) 경과=분실·미도착. 화주가 고르지 않고, 넣을 때 제한 정책(`reviews_insert_outcome`)이 선적 기록과 같은지 본다. 0007 전 후기(outcome 빈 칸)는 고쳐 쓰지 않고 읽을 때 선적 기록으로 채운다.
- v2 trust — 업체 화면 후기는 최근 6건에 더해 회송·반려·분실로 끝난 최근 후기 4건을 따로 읽어 합친다 — 나쁜 끝의 후기가 좋은 후기에 밀려 첫 화면에서 사라지지 않게.
- v2 trust — 업체 공개 답변(`review_replies`)은 새 판(supersedes_id, 판 번호 +1, 한 판을 잇는 판은 하나)으로만 쌓고 UPDATE·DELETE 권한이 없다. 공개 화면에는 현재 판과 「고친 판(vN)」 표시만. 데모 걷어내기는 후기 → 선적 → 조직 CASCADE 로 함께 지워지고, `DEMO_TABLES` 에 표를 더했다.
- v2 trust — 업체 화면 390 폭 가로 밀림(656px)은 회사 정보 칸이 한 열 그리드의 자동 폭을 넓히던 것(이전부터). 그리드 열을 `minmax(0,1fr)` 로 묶어 고쳤다.
- v2 assure — 확정가 = 같은 조건 총액의 중간값 + 초과 위험 프리미엄. 초과 위험은 신뢰수준 경험 분위(선형 보간)와 정규 근사(z·표준편차) 중 큰 쪽 − 중간값, 배수를 곱하고 최소 프리미엄·「표본 적음」(기본 5곳 미만) 최소 프리미엄으로 받친다. 분산 0 이면 최소 프리미엄만. 프리미엄이 상한(기본 20%)을 넘으면 확정가를 내지 않는다. 표본은 대표 업체(특수관계 뺀 가장 싼 곳)와 같은 운송 방식의 요금표·응찰 총액(방식이 섞이면 분포가 뜻을 잃는다). 순수 함수 `money/assure.ts`, 요율은 `v2.firm_price_rates`.
- v2 assure — 회송 보장료 = 보장 금액(국내 창고 + FC 운송 구간) × 신뢰도 가중 회송률((회송 + K·시장 기본값)/(물량 + K)) × (1 + 부가율), 최소 보장료·올림. 회송률이 한도를 넘으면 보장 대상 아님. 후불은 30일당 bp 참고 수수료만. 요율은 `v2.coverage_rates`·`v2.deferred_rates`.
- v2 assure — 스위치 넷(`v2.firm_price_enabled`·`coverage`·`deferred`·`consolidation`)은 참조 시드에서 false, 키가 없거나 값이 정확히 true 가 아니면 꺼짐으로 읽는다. 운영 설정 화면에서 켜고 끄는 것도 설정 새 판(이유 필수·감사 기록). 켜도 계약·결제·보장·외부 연락은 없고, 확정가는 「시범 견적 기록」(`firm_price_quotes`, UPDATE 없음·새 판)까지만, 나머지는 「시범 참여 신청」 = 관심 등록. 화면에 「사람이 정할 일: 주선업 등록·보험사·금융사 제휴」.
- v2 assure — 관심 등록(`assure_interests`)은 한 사람 한 종류 한 번(유일 색인), 본인·운영자만 본다, 고치거나 지울 권한이 없다. 시범 확정가 금액은 화면이 보낸 값을 믿지 않고 서버가 같은 조건으로 다시 셈해 기록한다. 두 표 모두 조직 CASCADE 로 데모 걷어내기에 함께 사라진다(`DEMO_TABLES` 에 덧붙임).
- v2 metrics — 이벤트(`fcd.events`, 0010)는 행동이 끝난 뒤 따로 한 줄 쓴다(`recordEvent`, asUser). 기록이 실패해도 행동을 되돌리지 않고 로그만 남긴다 — 기존 액션의 흐름·결과를 바꾸지 않으려고. 금액은 이벤트에 복사하지 않고 지표가 대상(응찰·청구·선적)을 따라가 원래 기록에서 읽는다. 쌓기만 하고 UPDATE·DELETE 권한이 없다. 쓰기 정책은 「본인이 자기 조직 이름으로, 걸린 셀러만」.
- v2 metrics — 데모 이벤트는 따로 지어내지 않고 이미 넣은 데모 자료(요청·응찰 첫 판·예약·5단계/9단계 이력·회송·청구 판·평가·조직 생성)에서 SQL 로 만든다(`seed/demo/events.ts`, 멱등). 데모가 이미 있는 DB 에도 이벤트가 없으면 채운다.
- v2 metrics — 지표 정의: 월간 활성 셀러 = 기간 안 셀러가 걸린 이벤트가 있는 곳 · 관리 선적 = 플랫폼 예약 수 · 재선적률 = 기간 안 예약 셀러 중 그 전에도 예약한 적이 있는 곳 · 견적 대비 청구 차이 = 선적마다 최신 판 청구 ÷ 고른 응찰 − 1 의 평균 · 회송률 = FC 입고 선적의 회송 수량 ÷ 수량 · 선적당 매출 = `commissionAmount`(물류비 − 관세사 보수, 요율은 설정) ÷ 예약 수. 계산은 `src/lib/metrics.ts` 순수 함수.
- v2 metrics — 초대로 들어온 업체 비율은 가입 이벤트의 `detail.via = 'invite'` 로 센다. 이 가지에는 초대 표가 없어 지금은 늘 0% · 「초대 기록 준비 중」으로 보인다(workspace 꾸러미가 초대 가입 때 `recordSignup(…, 'invite')` 를 부르면 채워진다).
- v2 metrics — 목적지 넓히기(0011)는 새 표 대신 `fcd.fc_centers` 에 `kind`(coupang_fc·3pl·mall_wh)·`is_example`·`ord` 칸을 더했다 — 견적 요청·선적의 `fc_code` 외래키와 기존 쿼리를 그대로 쓰려고. 예시 창고 여섯 곳은 실제 회사 이름 없이 「예시 3PL 창고 · 지역」처럼 지역만 적는다. 코드는 FC-·TP-·MK- 로 가른다.
- v2 metrics — 업체 요금표의 「FC 운송」은 쿠팡 FC 입고 운송·예약 기준이라, 쿠팡 FC 가 아닌 목적지에서는 그 칸을 도착항→목적지 거리 기준 참고치(팔레트 × (기본 + km × km당), 최저요금 — 설정 `destination_leg`)로 바꿔 넣고 참고치로 표시한다(확정 합계 밖). 비교·공개 계산기·물류사 자동 응찰 금액이 같은 순수 함수(`money/destination.ts`)를 쓴다. 쿠팡 FC 는 예전 계산 그대로(시험으로 고정).
- v2 workspace — 서류함 칸은 `documents.kind` 의 check 를 넓히지 않고(DROP CONSTRAINT 를 쓰지 않으려고) `documents.shelf` 칸을 덧붙였다. 비어 있으면 kind 로 가른다(SQL `fcd.doc_shelf` = TS `shelfOf`, 시험으로 같음을 본다). 쿠팡 바코드 PDF 는 kind `other` + shelf `coupang_barcode`.
- v2 workspace — 한눈 타임라인은 표준 9단계를 셀러 일곱 마디로 접는다: 예약=1 · 출항=5 · 입항=6 · 통관=7 · FC 입고=9 단계, 청구=현재 판 청구서 + 화주 결정. 사이 단계(2~4·8)는 다음 마디의 「지금」 설명. 9단계 띠는 그 아래 접어 둔다.
- v2 workspace — 청구 승인/이의는 덧붙이는 표 `invoice_decisions`(UPDATE·DELETE 권한 없음). 결정을 바꾸면 `supersedes_id` 새 판, 청구서 한 장에 첫 판 하나. 정정된 옛 판 청구서에는 못 남긴다. 이의는 사유 5자 이상이고 물류사 쪽 예외(청구 편차)로도 연다 — 화주 선적 화면의 예전 「청구 확인 요청」 폼은 이것으로 바꿨다. 「차이 큼」 기준은 설정 `workspace.billing_flag_bp`(첫 판 300bp), 차이 계산은 `money/billing-diff.ts`.
- v2 workspace — 거래처 초대 토큰은 32바이트 난수, DB 에는 sha-256 만(`partner_invites.token_hash`, fcd_user 는 이 칸을 읽을 권한이 없다). 링크는 만들 때 한 번만 화면에 보이고 발송은 하지 않는다(OUTBOUND 꺼짐). 유효 일수는 설정 `workspace.invite_days`(14). 한 번 쓰면 끝, 화주는 거두기(revoked_at)만 고칠 수 있다.
- v2 workspace — 초대 받기는 security definer 함수 `fcd.accept_partner_invite` 하나로만(연결 표 `shipper_partners` 에는 INSERT 권한이 없다). 가입 때는 `asSystem(fn, 새 사용자 id)` 로 같은 트랜잭션에서 소속을 만든 뒤 이 함수를 부른다. 이미 가입한 물류사 관리자는 링크 화면에서 「거래처로 연결」. 링크 열람은 `fcd.invite_lookup`(상태·화주 이름·받는 곳 이름·만료만).
- v2 workspace — 거래처로 연결돼도 물류사가 화주 조직 이름을 보는 규칙(orgs RLS: 예약 뒤에만)은 넓히지 않았다. 물류사는 초대 화면·알림에서만 누가 불렀는지 본다. 데모 시드는 알림을 더하지 않는다(데모 알림 200건 기준 시험).
- v2 검토 고침 — 공개 「구간 시세로 도착원가」(`/api/tools/arrival`)는 비공개 요금표까지 asSystem 으로 모으므로 최소 표본을 둔다: 설정 새 키 `tools.arrival_rule`(첫 판 minSamples 3 · minSpreadSamples 5 · perMinute 60). 요금표를 낸 업체가 3곳 미만이면 업체 집계 대신 플랫폼 참고치(`basis: 'reference'`)로, 요금표가 5장 미만이면 최저·싼 쪽 4분의 1 을 싣지 않는다. 분당 횟수도 이 키에서 읽는다(코드에 60 을 박지 않는다). 키가 없으면 오류로 멈춘다(참조 시드를 올려야 한다).
- v2 검토 고침 — 청구서 점검 분포는 표본 3~4장에서 네 분위(최저·싼 쪽 25%·중간값·비싼 쪽 25%)를 다 내면 요금표 금액이 거의 그대로 드러난다. `invoice_check_rule.minSpreadSamples`(첫 판 5) 미만이면 중간값만 싣고, 그때 「과함」은 비싼 쪽 경계 없이 중간값 +20% 로 본다. 이 칸이 없는 옛 판 설정이면 퍼짐을 싣지 않는 쪽으로 읽는다.
- v2 검토 고침 — 붙여넣기 금액 앞 통화(「해상운임 USD 1,200」「O/F⇥USD⇥850」)도 읽고 항목 이름에서 뺀다. 줄 이름에 방식(LCL·FCL·항공·카페리)이 하나만 보이고 방식이 「상관없음」이면 그 방식으로 미리 고르며, 「상관없음」으로 낸 결과 위에는 「방식이 섞인 비교입니다」를 띄운다. 결과를 낸 뒤 입력을 고치면 「조건이 바뀌었습니다」와 함께 보관을 막는다.
- v2 검토 고침 — 0012(`0012_v2_access_fixes.sql`): 기존 정책·함수를 지우지 않고 좁혔다. 끝·후기 함수(`shipment_outcome`·`review_outcome`·`review_partner`)는 볼 수 있는 선적·후기에만 값을 내고, 보는 사람 조건 없는 속 함수 `shipment_outcome_raw` 는 누구에게도 실행 권한이 없다. `partner_trust_facts` 는 목록에 오른 업체(또는 본인 조직·운영자)만. 초대 거두기는 제한 정책으로 「아직 안 거둔 초대만, 지금 이전 시각만」. 이벤트 쓰기는 제한 정책으로 대상(선적·견적 요청)을 볼 수 있고 셀러가 그 대상의 셀러일 때만, 가입은 자기 조직만.
- v2 검토 고침 — 초대 업체 비율의 분모는 기간 안 **물류사** 가입만(초대는 물류사에게만 간다). 「준비 중」 표시는 없앴고, 데모 시드는 초대 링크로 연결된 물류사의 가입을 `via: 'invite'` 로 적는다. 「차이 큼」 기준은 청구 승인·평가 기본값·운영 지표 모두 `workspace.billing_flag_bp` 하나로(평가 기본값 3%, 지표 5% 를 박아 두던 것을 바꿨다). 물류사 청구서 등록 때 여는 예외(응찰 대비 5%)는 v1 흐름이라 그대로 뒀다.
- v2 검토 고침 — 청구 이의는 그 선적에 열린 청구 편차 예외가 없을 때만 새로 연다(겹치지 않게, 새 사유는 알림으로). 청구 이벤트(`invoiced`)에는 금액을 싣지 않는다(0010 의 원칙). 관심 등록은 `on conflict do nothing` 으로 두 번 눌러도 오류 없이 한 줄.
- v2 검토 고침 — 어드민 설정 화면에서 v2 키(tools.* · invoice_check_rule · workspace.* · destination_leg)도 새 판으로 바꿀 수 있게 검사 규칙을 더했다(`src/lib/v2-setting-schemas.ts`). 추천 점수 넷째 항목의 이름은 「가격 확실성」 하나로(계산기·비교·어드민·정책·자주 묻는 질문의 「가격확정도」를 바꿨다, `SCORE_TERMS`).
- v2 검토 고침 — 공개 제한의 IP 는 플랫폼이 채우는 머리글(x-vercel-forwarded-for → x-real-ip)을 먼저 본다. 제한 횟수 기억은 여전히 인스턴스별 메모리다(여러 인스턴스면 그만큼 느슨해진다 — 공유 저장소는 나중 일).
- v2 검토 고침 — 미리보기 띠는 높이를 1.75rem 한 줄로 고정하고 붙박이로 두며, 띠가 있으면 `--banner-h` 만큼 셸 머리·옆 메뉴·공개 머리가 내려 앉는다(`:root:has([data-preview-banner])`). 띠가 없는 운영 화면은 0 이라 그대로다.
- v2 마지막 점검 — 캡처 목록(`scripts/shots-all.mjs`)에 v2 새 화면(`/check`·`/tools/pnl`·`/app/checks`(+상세)·`/app/docs`·`/app/partners`·`/partner/reviews`·`/admin/metrics`·`/admin/assure`)을 덧붙였다. 390/768/1440 에서 가로 밀림은 없었다.
- v2 마지막 점검 — 서류함(`/app/docs`)은 칸마다 최근 12건만(예전 40건 × 다섯 칸이라 390 폭에서 15,000px 이 넘었다). 나머지는 선적 화면 서류 탭에서 본다는 안내는 그대로.
- v2 마지막 점검 — 추천 점수 항목(`ScoreBreakdown`)은 창 폭이 아니라 담는 칸 폭(컨테이너 쿼리 `@md`)으로 세 칸/한 칸을 가른다 — 물류사 후기 화면 옆 칸(340px)에서 설명이 조각나던 것.
- v2 마지막 점검 — 차트 축의 원 단위 눈금은 10만 원 밑이면 소수 한 자리 만(4.5만)으로 — 「5만·3만·2만」처럼 반올림돼 간격이 틀려 보이던 것. 지표 칸의 「지난 기간 대비」는 한 덩어리로 줄을 바꾼다.
- v2 마지막 점검 — `/admin/assure` 는 데모에서도 처음엔 비어 있다. 데모 관심 등록을 시드하면 e2e 의 「1명」 확인과 겹치고, 빈 상태 글이 어디서 등록되는지 알려 주므로 그대로 뒀다.
- v2 wing — 쿠팡 WING 연동은 **판매자 본인 키 + 읽기만 + `WING_ENABLED` 꺼짐**으로 시작하고, 연동 업체(솔루션사) 등록은 병행 신청 대상으로 남겼다(docs/wing-plan.md §4). 코드는 두 길을 모두 받는다(`wing_connections.method`). 쓰기 API 는 어댑터에 만들지 않았다(GET 만).
- v2 wing — 로켓그로스 입고 요청·바코드 라벨 API 는 공개 여부를 확인하지 못해(쿠팡 문서 사이트가 이 환경에서 막힘, 솔루션사 안내는 「입고 생성은 윙에서」) 첫 판의 실제 경로를 **WING 에서 내려받은 입고 목록 파일 올리기**로 두었다. HTTP 어댑터의 `listInboundRequests` 는 켜져 있어도 `WingUnsupportedError`, 재고 요약 경로만 문서 요약대로 둔다.
- v2 wing — 서명은 `signed-date(UTC yyMMddTHHmmssZ) + METHOD + PATH + QUERY('?' 없이)` 의 HMAC-SHA256 16진. 쿠팡 문서의 입력→서명 예시 값을 찾지 못해 시험은 RFC 4231 시험 2(HMAC 자체)와 규칙 고정으로 했다. 쿼리 문자열은 서명·전송에 같은 문자열 하나를 쓴다(두 번 만들지 않는다).
- v2 wing — 키는 AES-256-GCM(AAD = 조직 id)으로 잠그고 `WING_KEY_ENCRYPTION_KEY`(32자 이상)에서 SHA-256 으로 키를 만든다. 암호화 키가 없으면 키를 **받지 않는다**(평문 대체 경로 없음). 평문은 업체 코드·Access Key 끝 4자리만, Secret Key 는 끝자리도 남기지 않는다.
- v2 wing — 암호문 칸(`wing_connections.key_blob`)은 fcd_user 에 SELECT 권한이 없고(칸 단위), 보안 정의 함수 `fcd.wing_key_blob` 로만 꺼낸다 — 그 조직 사람 · 현재 판 · 폐기 아님일 때만, 꺼낼 때마다 `key_decrypted` 접근 기록. 운영자(플랫폼)도 못 꺼낸다. `key_decrypted` 기록은 사람이 직접 쓰지 못한다(정책).
- v2 wing — 키 지우기 = `status = revoked` · 암호문 없는 새 판(UPDATE·DELETE 권한 없음). 옛 판 암호문이 DB 에 남는 한계는 「WING 에서도 키를 지우세요」 안내로 막고, 암호 파쇄·칸 비우기(지우는 SQL 필요)는 사람이 정할 일로 남겼다.
- v2 wing — WING 이벤트(wing_imported 등)는 `fcd.events` 가 아니라 `fcd.wing_access_log` 에 쌓는다. `events.kind` 의 check 를 넓히려면 DROP CONSTRAINT 가 필요하고, 2차 꾸러미 여럿이 같은 제약을 각자 다시 만들면 뒤에 합친 쪽이 앞의 종류를 지운다. 이름·action 짝은 `src/lib/metrics.ts` 의 `WING_EVENT_KINDS`·`WING_EVENT_ACTION`. 기록은 행동과 같은 트랜잭션에서 쓴다(키 저장 기록이 빠지지 않게).
- v2 wing — 짝 제안 점수 = FC 같음 40 · 날짜 30 · 수량 30(곧게 줄어듦, 기준치는 설정 `wing.match_rule`), 한 선적에 한 입고 요청을 점수 높은 순으로 나눠 준다. FC 가 다르면 최대 60점이라 기본 기준(70) 미만 — 다른 FC 는 사람이 직접 고른다. **자동 확정은 없다.** 확정 때 점수는 화면 값을 믿지 않고 서버가 다시 셈한다.
- v2 wing — 짝(`wing_matches`)은 입고 요청 id 가 아니라 번호(`external_no`)로 가리킨다 — 다시 가져와 입고 요청 새 판이 생겨도 짝이 이어진다. 한 선적이 두 입고 요청과 동시에 짝이 되지 않게 `fcd.wing_match_ok` 가 막고, 액션은 넣기 전에 같은 함수를 물어 알아듣게 거절한다(넣다가 막히면 트랜잭션이 깨진다).
- v2 wing — 흉내 어댑터(데모·시험)는 sha-256 기반 결정적 난수, 번호는 `EX-RG-########`(예시). 화면의 「예시 입고 요청 가져오기」는 데모 조직에서만 — 실제 셀러 목록에 가짜가 섞이지 않게(지울 수 없는 표라). 데모 시드는 흉내 입고 요청 10건 안팎 + 짝 셋 + 접근 기록, 키 연결은 넣지 않는다(가짜 암호문도 두지 않는다).
- v2 wing — WING 파일 머리글은 실물을 못 봐 짐작한 별칭이다(화면에 「열 이름 확인 필요」). 기존 `ExcelImport` 에 선택 prop `allowMapping` 을 더해 「칸 잇기」(우리 칸마다 파일 머리글 고르기)를 붙였다 — 끄면 예전과 같다(SKU·요금표 올리기는 그대로).
- v2 wing — 바코드 PDF 는 짝 맞은 입고 요청 줄에서 올리고, 기존 `uploadDocument`(칸 `coupang_barcode:other`)를 그대로 불러 그 선적 서류함으로 넣는다. 파일 모양은 PDF 만.
- v2 wing — 실측 회송률 = 회송 ÷ (입고 + 회송)(bp 사사오입), 짝 맞고 결과 칸이 둘 다 있는 입고 요청만. 물류사가 선적에 적은 회송 수량과 나란히 보이기만 하고, 추천 점수·회송 보장료에는 아직 넣지 않았다(다음 단계).
- v2 wing — smoke 는 시험용 `WING_KEY_ENCRYPTION_KEY`(smoke-only 값)를 넣고 `WING_ENABLED` 는 비워 돌린다 — 키 저장 흐름은 보되 쿠팡은 부르지 않는다.
- v2 alliance — 확정가의 계약 주체는 **등록된 국제물류주선업체(제휴 주선사)**로 둔다(갈래 ①, 기획 `docs/alliance-plan.md`). 법률 검토에서 준비금 분담이 문제되면 ② 영업 대리(계약 판 `model = sales_agency`, 준비금·플랫폼 부담 0 — DB check 와 `termsModelOk`), 물량이 증명되면 ③ 직접 등록. 등록 요건(법인 자본금 3억 원·보증보험 1억 원·3년마다 등록기준 신고)은 검색 요약으로만 확인했다 — 이 환경에서 law.go.kr 등 원문 접속이 막혀 문서에 「원문 대조 필요」로 적었다.
- v2 alliance — 표 넷(0013): `alliance_partners` 는 제휴 후보·상태라 **상태 칸만** 운영자가 UPDATE(칸 단위 권한, 감사 기록은 앱), 요건·계약 판·정산 명세는 UPDATE·DELETE 없이 `supersedes_id` 새 판(한 판을 잇는 판은 하나, 요건은 칸마다 첫 판 하나, 현재 판만 이을 수 있음 — security definer 검사 함수). 물류사는 요건을 「올림(submitted)」으로만 넣고, 확인·반려 판은 운영자만. 명세의 `net_payable = 수수료 + 부가세 + 준비금 적립 − 플랫폼 부담` 은 DB check 로도 막는다.
- v2 alliance — 화주는 제휴 표를 읽지 못한다. 확정가 카드의 「계약 상대」는 security definer 함수 `fcd.alliance_contract_party` 하나로 이름·등록번호 끝 4자리·계약 번호·유효일만 받는다. 조건: 스위치 `v2.alliance_enabled` 켜짐 · 제휴 중 · 데모 숨김 규칙 · 필수 요건(설정 `alliance.rules.requiredKinds`) 현재 판이 모두 확인함·만료 전·보증보험 최소 금액 이상 · 서명한 계약 판이 오늘 유효. 비교 1위(특수관계 뺀 대표 업체)가 조건을 채우면 그 업체, 아니면 가장 최근 서명한 제휴사 — 이때 「비교 1위 업체와 다를 수 있음」을 적는다. TS 체크리스트(`alliance-check.ts`)가 같은 규칙이고 시험으로 맞춘다. 대표 업체를 넘기려고 assure 의 `lead` 에 `partnerId` 칸 하나를 더했다.
- v2 alliance — 책임 나누기는 귀책 셋(셀러·주선사·외부 요인)으로 가르고, 비율은 사건(초과비용·회송·분실·지연)별로 계약 판 `liability` 에 둔다. 외부 요인만 플랫폼 비율과 **건당 확정가 대비 상한**을 적용하고, 플랫폼 몫은 준비금에서 먼저, 모자라면 「준비금 부족」(플랫폼 자기 돈). 첫 판 기본값(설정 `alliance.default_terms`): 수수료 3%(확정가 − 관세사 보수 — 관세사법 원칙 그대로), 준비금 프리미엄의 30%, 초과비용 플랫폼 50%·상한 3%, 나머지 사건 0%. 계산은 `money/alliance.ts`, 기획 문서 8장 숫자는 `simulateAlliance` 시험으로 고정.
- v2 alliance — 정산 명세는 운영자가 선적 줄을 붙여 넣으면(탭·쉼표, `alliance-lines.ts`) 서버가 **현재 서명한 계약 판 요율과 설정 부가세율로 다시 셈**한다(화면 미리보기는 같은 순수 함수). 기초 준비금 = 앞 기간 발행 명세의 기말. 세금계산서 번호·상태를 바꾸면 같은 줄을 다시 셈한 새 판. 선적 기록과 자동으로 잇지는 않았다(확정가 계약이 아직 없어 원가 신고 흐름이 없다).
- v2 alliance — 스위치가 꺼져 있으면 운영 화면은 준비용으로 다 쓰고, 물류사 화면은 안내·자기 기록만(신청·서류 올리기 잠김, 서버 행동도 거절), 화주 카드는 「제휴 주선사 확정 전」. 켜도 계약서 서명·돈의 이동·외부 연락은 없다.
- v2 alliance — 요건 서류 파일은 기존 서류함과 같은 저장소 방식(비공개 버킷 `fcd-docs`, 서버 열쇠로만 올리고 60초 서명 주소)을 `src/lib/server/doc-storage.ts` 로 묶어 썼다. 기존 `actions/docs.ts` 는 다른 꾸러미와 겹치지 않게 건드리지 않았다. 서버 행동 본문 한도가 Next 기본 1MB 라(기존 서류 올리기도 같다) 화면에 「1MB 이하」로 적었다 — 한도를 올릴지는 배포 환경(요청 본문 한도)과 함께 사람이 정한다.
- v2 alliance — 데모: 가람해운항공(제휴 중 · 보증보험 20일 뒤 만료로 30일 경고 · 계약 v1 초안 → v2 서명 · 지난달 정산 v1 → v2 세금계산서 번호)과 블루웨이브혼재(요건 확인 중 · 보증보험 금액 부족 반려 뒤 다시 올림). 번호·증권은 「예시」. 데모 물류사 계정(한바다포워딩)은 비워 두어 화면에서 신청해 볼 수 있다. `DEMO_TABLES` 에 네 표, 걷어내기는 조직 CASCADE. assure 시험의 「v2.%_enabled 스위치 넷」은 「넷 이상, 모두 꺼짐」으로 넓혔다.
- v2 interview — 셀러 인터뷰는 제품 안 과제형 흐름(`/interview/[token]`, 로그인 없음) + 인터뷰어 모드(`/admin/research/[id]/conduct`) + 결정 보드(`/admin/research`)로 한다(기획 `docs/research-plan.md`). 설문 도구만으로는 셀러 자기 조건으로 계산한 확정가·점검·판매손익을 보여 줄 수 없어서. 링크는 화면에 한 번 보이고 복사해 전한다(발송 코드 없음).
- v2 interview — 판정선은 설정 `research.rules`(참조 시드 첫 판, 어드민 설정 화면에서 새 판). 방안 A = 반대 질문(「지금처럼 받고 나중에 따지는 편이 낫다」에 「그렇다」)을 통과한 +3% 이상 「예」가 **절반 초과**(같으면 미충족)·12명 이상. 말로 한 의향은 옆에 적기만 하고 판정에 쓰지 않는다. 윌슨 95% 아래 끝까지 넘으면 「강한 충족」.
- v2 interview — 사다리는 낮은 값부터 묻고 처음 「아니오」에서 멈춘다(위는 아니오로 본다). 「아니오」 뒤 「예」는 이어진 「예」까지만 인정하고 「모순」으로 센다(보수적으로). 계산은 `money/research.ts`.
- v2 interview — 동의는 참여자 칸을 고치지 않고 `research_consents` 에 쌓는다(동의·거부·철회 한 줄씩, 지금 상태 = 최근 줄). 마이그레이션에 `update fcd.… set` 을 두지 않는 규칙(db 시험)과, 동의 증빙을 남기는 쪽이 맞아서. 같은 까닭으로 「링크를 열어 봄」 시각은 따로 적지 않고, 동의 기록이 있으면 「동의함(답 전)」으로 본다.
- v2 interview — 응답자는 표 권한이 없고 security definer 함수 셋(`research_invite_open`·`research_consent`·`research_save`)으로 자기 토큰의 자기 응답만 이어 읽고 새 판을 쓴다. 끝낸 링크는 닫히고 답을 더는 돌려주지 않는다. 운영은 인터뷰어 모드(source = interviewer)로만 쓰고, 참여자마다 한 줄기(뿌리 하나·다음 판 하나, `research_chain_ok`).
- v2 interview — 연락처는 선택이고 표의 칸 권한에서 뺐다. 운영도 가린 값(`v_research_participants.contact_masked`)만 보고, 풀어 볼 때는 `fcd.research_contact` 가 감사 기록을 남긴다. 「연락처 지우기」는 칸을 비운다(앱 행동). 보관 기간 지난 답변 삭제는 사람이 SQL 파일로.
- v2 interview — 실험 ②(청구서 점검 업로드 비율)는 `fcd.events` 가 아니라 새 표 `check_funnel_events` 에 쌓는다. `fcd.events` 는 조직이 필수(org_id not null)이고 종류 칸이 check 제약이라 넓히려면 DROP CONSTRAINT 가 필요해서. 기기 번호는 브라우저 무작위 값의 sha-256 만(IP·계정·청구서 내용 없음), 쓰기는 `record_check_event` 함수로만, 데모 행만 데모 운영 조직에 매단다(걷어내기 CASCADE). 방문 → 입력(방법별) → 점검 → 보관, 비율 = 점검까지 간 기기 ÷ 방문 기기.
- v2 interview — 인터뷰 미리 계산은 공개 도착원가와 같은 규칙(공식·인증 대기 업체, 업체가 적으면 플랫폼 참고치, 요금표가 없으면 참고치)으로 집계만 싣고, 확정가 카드는 기준·프리미엄·확정가·표본 수만(최저·경계값 없음). 청구서 점검은 지난 물류비 총액 한 줄을 그 중간값과 `invoice_check_rule` 판정선으로 견준다(항목이 없어 구간별 판정은 하지 않는다). 셀러가 넣은 CBM 으로 기준 화물을 비례해 늘린 예시 화물로 계산한다.
- v2 interview — 실험 ③ 물량 단가는 CBM 당 원 단위 하나로 받고, 포함 범위(해상+CFS·도착항까지·FC 입고까지)가 다르면 곡선·판정에 섞지 않는다. 고친 단가는 새 판(옛 판은 곡선에서 빠짐).
- v2 합치기 — 데모 시드의 「오늘 갱신」 요금표 created_at 을 그날 UTC 자정 아래로 내려가지 않게 묶었다(자정 직후 verify 가 오늘 갱신분 0 으로 떨어지던 시계 의존 실패). 난수 뽑는 횟수는 그대로.
- v2 2차 고침 — 「제휴 중」 바꾸기와 화주 카드의 계약 상대를 한 SQL 함수 `fcd.alliance_ready` 로 판정한다(요건·만료·보증 금액·서명한 판 오늘 유효·업체 플랫폼 상태 공식/확인 중). 두 화면이 다른 답을 내지 않게. 특수관계 업체는 계약 상대 순서에서 뒤로(빼지는 않음).
- v2 2차 고침 — 정산 명세의 새 판은 앞 판의 계약 판(terms_id) 그대로 다시 셈하고 DB(`alliance_settlement_chain_ok`)도 다른 판을 막는다. 요율이 바뀐 뒤 세금계산서 번호만 적어도 금액이 바뀌던 문제. 다른 요율이 필요하면 새 명세. 새 명세는 그 기간에 걸친 서명한 판(끝난 판도 서명일이 있으면).
- v2 2차 고침 — 제휴 신청·요건 올리기, WING 키 저장·폐기·꺼냄(= 쿠팡 바로 가져오기)은 그 조직 관리자만(제한 정책 덧붙임). 거래처 초대와 같은 규칙. 파일 가져오기·짝 맞추기는 구성원 누구나.
- v2 2차 고침 — 인터뷰 「끝남」은 한 번이라도 끝낸 판이 있으면 끝남(보드·링크 함수·동의 함수가 같은 규칙). 끝낸 뒤 운영이 대신 고치면 답만 새 판, 완료·done 은 유지.
- v2 2차 고침 — 결정 보드는 예시 자료를 기본으로 뺀다. 데모 운영 계정으로 볼 때만 기본 포함, 「예시 포함/빼고」 토글. 판정 표본은 끝낸 인터뷰만, 반대 질문 빈 답은 확인 안 된 의향.
- v2 2차 고침 — 사다리 바로 앞 화면의 확정가 카드에서 기준 총액·프리미엄 %를 뺐다(금액만). %는 사다리에 답한 뒤에만 보인다 — 앞 화면의 숫자가 사다리 답의 기준점이 되지 않게.
- v2 2차 고침 — 동의 문구를 실제와 맞췄다: 자동 삭제 없음(운영 담당이 지움) · 철회·삭제 요청은 링크를 보낸 담당자에게 · 거부도 한 줄 기록 · 연락처는 모집 때 받은 것. 보드에 「지울 대상」, 인터뷰어 모드에 「철회·삭제 요청 받음」(쌓기만). 실제 삭제 SQL 은 사람이.
- v2 2차 고침 — WING 키 만료 알림 시작 일수를 설정 `wing.key_warn_days`(첫 판 14)로 뺐다. 암호화 키는 공백·반복 글자가 많은 값을 받지 않는다(무작위 32바이트 권장). 3차 마이그레이션 번호는 0017_sales · 0018_sourcing 으로 민다(0016 은 2차 고침).
- v2 2차 마지막 점검 — 캡처 목록에 2차 화면(`/admin/alliance`·`/partner/alliance`(+中)·`/app/integrations/wing`·`/admin/research`(+인터뷰어 모드)·`/interview/[token]` 동의·선적 단계)을 두고 390/768/1440 × 밝음/어두움으로 찍었다. 가로 밀림 0. WING 화면은 「연동 준비 중」 표가 머리와 가져오기 칸에 두 번 나오던 것을 머리 하나로 줄이고, 꺼져 있을 때 「바로 가져오기」 옆에 이유를 한 줄 적었다. 결정 보드의 대상 표는 390 폭에서 표 안에서 가로로 넘긴다(다른 어드민 표와 같은 방식 — 카드로 바꾸지 않음).
- v2 3차 sourcing — 소싱처 찾기는 「패밀리 사이트」로 따로 둔다(`/family/sourcing`, 이름·도메인은 가칭 — 사람이 정함). 계정·9구간 원가 엔진·구간 시세는 FC도착과 공유. 머리는 종이색 + 청록 띠(역할 토큰 안에서 「다른 방」), 새 색을 만들지 않았다.
- v2 3차 sourcing — 스위치 `sourcing.enabled`(첫 판 꺼짐). 꺼져 있어도 요청은 기록만(`preview = true`) — 관심 등록과 같은 규칙. 켜도 앱은 공급처·화주에게 연락하지 않는다(발송·외부 호출 없음).
- v2 3차 sourcing — 1단계 자료원은 현지 소싱 담당 수동 조사 + 셀러가 붙인 링크를 담당이 확인. 1688·알리바바·타오바오 공식 API 는 약관·계약·권한 확인 전이라 제공자 인터페이스만 두고 흉내 제공자(「예시 공장 A」, FNV 해시 결정적)만 만들었다. 공식 제공자 id 는 「준비 중」 오류.
- v2 3차 sourcing — 후보 도착원가는 새 식을 만들지 않고 `candidateSim` → `sellerPnl`(판매손익) 재사용. 물류비는 같은 화물(수량 × 개당 무게·부피·박스 입수)로 `compare` → 공개 도착원가와 같은 최소 표본 규칙(`tools.arrival_rule`), 요금표가 없으면 플랫폼 참고치. 대행 수수료(가정치 `sourcing.fees.agentFeeBp`)는 추가비용 칸.
- v2 3차 sourcing — 요청 상태·담당은 덧붙이는 기록(`sourcing_request_events`), 후보 조건은 `candidate_quotes` 새 판(내리기도 새 판). 후보·조건은 운영자만 넣는다. 화주가 남기는 상태는 「취소」뿐. 다섯 표 모두 UPDATE·DELETE 권한 없음.
- v2 3차 sourcing — 유사도는 낱말(낱말 + 한글·한자 두 글자 묶음 Dice)·분류(관세 분류 같음)·가격대(기대 매입가 = 목표 판매가 × 30% ± 30%) 가중 합, 없는 항목은 빼고 다시 나눔. 이미지 비중은 자리만(0). 정렬·참고용이며 같은 상품·지식재산 판단이 아니라고 화면에 적는다.
- v2 3차 sourcing — 인증은 「주장」(`certs_claimed`)과 「담당 확인」(`certs_verified`)을 나눠 싣는다. 흉내 후보는 중국 인증(CCC·CE)만 주장하고 KC 는 넣지 않는다.
- v2 3차 sourcing — sales 꾸러미의 표에 기대지 않는다. 시작점은 `src/lib/sourcing/seeds.ts` 의 `salesProductsForSourcing()`(지금은 빈 목록) + 저장한 SKU. 합칠 때 이 함수 안만 채운다. 2차 wing 자료에는 상품 이름이 없어 시작점으로 쓰지 않았다.
- v2 3차 sales — 판매 분석은 **키를 맡긴 화주 조직(현재 연결이 폐기 아님)만** 연다. 데모 조직은 가짜 키 암호문을 두지 않는 2차 원칙 그대로, **흉내 동기화 기록이 있으면 「예시 연결」**로 열고 모든 화면에 「예시」. 연결이 없거나(권유) 키는 있지만 가져온 기록이 없으면(시험 모드) **저장하지 않는 예시 미리보기**를 화면에서만 만든다 — 실제 셀러의 표에 예시를 넣지 않는다. 기획 `docs/sales-plan.md`.
- v2 3차 sales — 판매 자료 표 여섯(0017: 동기화 기록·상품·주문·재고 스냅숏·반품·정산)과 동의·만료 알림 표 둘. 주문은 하루 묶음 줄을 허용(`order_count`)하고, 상품·주문·반품·정산은 번호마다 한 줄기 새 판(`supersedes_id`), 재고는 쌓기만(상품·날짜의 최근 값 = 뷰). 개인정보 칸 없음. 정산 표는 쿠팡 응답 모양을 확인하지 못해 **표만 두고 `verified` 기본 거짓**, 데모 자료 없음.
- v2 3차 sales — 실제 호출은 2차 `WingHttpAdapter` 를 그대로 쓴다. 로켓그로스 주문·반품·상품 응답 칸 이름을 원문으로 확인하지 못해 **파서를 짓지 않았다**(`SalesHttpSource.fetchDataset` = 꺼짐이면 `WingDisabledError`, 켜져도 `WingUnsupportedError` · 키도 꺼내지 않음). 「연결 시험」만 켜졌을 때 재고 요약 GET 한 번(200 → 확인됨 새 판 · 401/403 → 확인 실패 새 판). 꺼졌거나 데모면 「시험 모드」 — 동의·키·암호화 키 지문·만료·연동 IP 만 본다.
- v2 3차 sales — 「읽는 것·하지 않는 것」 동의(판 `sales-consent-2026-09(법률 검토 전)`)를 `wing_consents` 에 쌓고, **키 저장 서버 행동이 현재 판 동의를 요구**한다(2차 `saveWingKey` 에 한 줄). 동의는 키를 넣는 사람과 같게 화주 관리자만. 화면은 동의 전에 키 칸을 닫는다(기존 키가 있거나 관리자가 아니면 그대로 보임).
- v2 3차 sales — 키 만료 알림: `wing.key_warn_days`(14) 안이면 「곧 만료」, 지나면 「만료」를 **(조직·발급일·종류)마다 한 번** 알림 표에 넣는다(`wing_key_alerts` unique, `asSystem` 좁게 — 알림 표는 원래 신뢰 경로만 쓴다). `notifyOrg` 가 돌려주는 밖으로 보낼 목록은 **보내지 않는다**(발송 없음). 연동 화면·판매 분석 화면을 열 때 판정한다(예약 작업 없음 — 화면을 안 열면 알림도 없다: 사람이 정할 일).
- v2 3차 sales — 개당 도착원가 = 그 SKU 로 올린 견적 요청의 선적 최근 N건(`sales.rules.actualShipments`, 청구서 현재 판 → 없으면 고른 응찰 합계) + 물품가(환율) + 관세 추정, 수량 가중. 없으면 구간 시세(비교 엔진 추천 순·특수관계 뺌, 9구간 합계 중간값 — 공개 도구와 같은 `summarizeArrival`). 개당 이익은 판매손익 계산기와 같은 `unitPnl`, 쿠팡 비용은 `tools.coupang_fee_basis`(예시 기준값 표시 그대로).
- v2 3차 sales — 재입고 권장일의 「구간 시세 운송일」 = 그 SKU 최근 선적 구간(없으면 기준 구간 YIW→ICN, 방식 무관)의 **오늘 유효한 요금표 `transit_days_max` 중간값**, 없으면 방식표 `days_max` 중간값. 준비일·덮을 일수·ABC 경계 등은 설정 `sales.rules`(제안값). 판매 속도는 품절이던 날도 0 으로 센다(보수적 — 사람이 정할 일 5).
- v2 3차 sales — 흉내 어댑터는 첫날(start)에 묶인 규칙이라 같은 첫날로 다시 만들면 앞날 기록이 같다 — 데모 「판매 기록 가져오기」는 첫 흉내 동기화의 시작일로 이어 만들어 바뀐 것만 새 판. 입고 끝난 데모 선적의 FC 입고일·수량을 1~4일 뒤 재고에 반영하고, 첫 136일은 재고가 떨어지면 예시 입고로 채운다(마지막 한 달 남짓은 채우지 않아 재입고 권장이 보인다). 마지막 SKU 는 판매가를 낮춘 「적자 SKU」 예시.
- v2 3차 sales — 「지금 견적 요청」은 권장 수량(하루 판매 × (운송 + 준비 + 덮을 일수) − 재고, 올림 단위)으로 SKU 의 박스·무게·부피·물품가를 비례해 늘려 **기존 견적 요청 화면에 채워 넘기기만** 한다(`quote-link.ts`). 대신 올리지 않는다.
- v2 3차 sales — 연동 화면 단계 안내는 여덟 단계(로그인 · 키 발급 · (선택) 로켓그로스 상품 API 동의 · 연동 IP · 동의 · 세 값 · 연결 시험 · 180일·끊기). 스크린샷 대신 메뉴 길 그림(칩)과 입력 칸 그림. 연동 IP 는 설정 `wing.egress_ips`(첫 판 빈 목록 → 「준비 중 — 운영이 정하면 표시」). 2차 e2e 의 단계 수(6→8)와 동의 한 번을 고쳤다.
- v2 3차 검토 고침 — 소싱 시작점 `salesProductsForSourcing()` 을 채웠다: 판매 분석과 **같은 열림 판정**(`salesAccess` — 키를 맡긴 조직·데모 예시 연결)일 때만 현재 판 상품을 최근 30일(어제까지, 취소 뺌) 판매량 순으로, 이어진 SKU 가 있으면 분류·개당 무게·부피·매입가도 그 SKU 에서. access none 은 판매 기록이 있어도 빈 목록. 상품별 행마다 「비슷한 상품 소싱」 링크(`/app/sourcing?from=sales:<옵션 번호>`).
- v2 3차 검토 고침 — 쿠팡을 실제로 부르기 전 문턱을 `src/lib/sales/gate.ts` 한 곳에(지금 판 동의 → 키 → 지금 암호화 키 → 만료 안 됨). 연결 시험의 실제 경로와 판매 기록 가져오기의 실제 경로가 같이 쓴다 — 2차에 동의 없이 넣은 키·동의 문구 판이 오른 뒤의 키·만료된 키는 **꺼내지도 않는다**(막힘 기록만).
- v2 3차 검토 고침 — 동의 문구에서 「운영자도 키를 볼 수 없습니다」를 빼고 사실대로(서버의 별도 암호화 키로 잠금·화면에 끝 4자리·운영 화면에 키 보는 기능 없음·꺼낼 때마다 기록) 고쳐 판을 `sales-consent-2026-09b` 로 올렸다 — 관리자가 다시 동의해야 새 키를 넣고 쿠팡을 부를 수 있다.
- v2 3차 검토 고침 — 입고 성과는 한 상품의 입고를 **선입선출**로 함께 잰다(`inboundReflectFifo`): 재고가 늘어난 날마다 그 증가분을 가장 앞 입고부터 나눠 주고, 앞 입고의 몫(수량×`inboundReflectBp`)이 채워져야 다음으로 넘어간다. 당일 반영(0일)은 제 입고일 때만 센다. 한 SKU 에 옵션이 여럿이어도 같은 선적은 한 번만(대표 = 먼저 나온 옵션). 중간값과 평균을 함께 보인다.
- v2 3차 검토 고침 — 최근 `velocityDays` 내내 품절이라 판매 0 이면 그 앞 창(최대 셋)의 속도를 쓴다(`velocityWithStockout`, 화면 「품절 전 속도」) → 품절 예상일 = 마지막 스냅숏 날, 「늦음」. 품절이던 날을 0 으로 세는 보수적 규칙(사람이 정할 일 5)은 재고가 남아 있을 때 그대로.
- v2 3차 검토 고침 — 상품 ↔ SKU 잇기 칸을 상품별 화면에 두었다(`linkSalesProductSku` — sku_id 만 바꾼 다음 판, 같은 조직·보관 안 한 SKU 만). 가져오기가 다시 돌아도 사람이 이은 연결은 이어 간다(store.ts 그대로).
- v2 3차 검토 고침 — 0019: 사용자 경로(`asUser`)로는 정산 `verified = true`·`source = 'api'` 판매 줄·「api 가져옴(ok)」 동기화 기록을 넣지 못한다(제한 정책). 'api' 상품 줄은 이름·옵션·판매가가 앞 판과 같은 다음 판(= SKU 잇기)만. **실제 쿠팡 가져오기를 만들 때는 신뢰 경로(`asSystem`, 좁게)로 쓴다.** 샘플 관심 등록은 내린 후보에 못 넣고, 기록(detail)의 판 번호·개당 도착원가는 서버가 지금 판 조건으로 다시 셈한다. 스위치가 꺼져 있으면 `preview = false` 요청을 못 넣고, 시작점 SKU·판매 상품은 같은 조직 것만.
- v2 3차 검토 고침 — 0018 의 프로필 FK(요청·상태 기록·후보·조건·샘플 관심의 created_by·actor_id·user_id)를 ON DELETE CASCADE → **SET NULL** 로 다시 걸었다(칸은 비어도 되게). 사람을 지워도 소싱 기록은 남는다. 넣을 때는 여전히 본인 id 여야 한다. 자료 줄은 지우지 않는다(제약만 바꿈).
- v2 3차 검토 고침 — 손익·상품별 표는 좁은 화면에서 개당 이익·마진을 상품 바로 옆 칸으로 옮기고 「옆으로 넘기면 … 칸이 더 있습니다」 한 줄을 둔다. 연동 화면은 키를 맡기기 전이면 좁은 화면에서 동의·키·연결 시험 묶음을 단계 안내 바로 아래로 올린다(`order-first`), 단계 5~7 에 「그 칸으로 가기」.
